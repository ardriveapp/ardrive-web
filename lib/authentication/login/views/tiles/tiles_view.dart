import 'package:flutter/widgets.dart';

class TilesView extends StatelessWidget {
  const TilesView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tiles View: Not Yet Implemented'),
    );
  }
}
