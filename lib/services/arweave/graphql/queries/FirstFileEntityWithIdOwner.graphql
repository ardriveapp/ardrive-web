query FirstFileEntityWithIdOwner($fileId: String!) {
  transactions(
    first: 1
    sort: HEIGHT_ASC
    tags: [
      { name: "File-Id", values: [$fileId] }
    ]
  ) {
    edges {
      node {
        owner {
          address
        }
      }
    }
  }
}
