query InfoOfTransactionsToBePinned($transactionIds: [ID!]) {
  transactions(first: 100, ids: $transactionIds) {
    edges {
      node {
        id
        owner {
          address
        }
        tags {
          name
          value
        }
        data {
          size
          type
        }
      }
    }
  }
}
