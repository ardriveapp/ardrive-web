query LicenseComposed($transactionIds: [ID!], $owners: [String!]) {
  transactions(
    first: 100
    ids: $transactionIds
    owners: $owners
  ) {
    edges {
      node {
        ...TransactionCommon
      }
    }
  }
}
