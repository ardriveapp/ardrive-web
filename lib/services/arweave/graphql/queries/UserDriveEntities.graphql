query UserDriveEntities($owner: String!) {
  transactions(
    first: 100
    sort: HEIGHT_DESC
    tags: [
      { name: "Entity-Type", values: ["drive"] }
    ]
    owners: [$owner]
  ) {
    edges {
      node {
        ...TransactionCommon
      }
    }
  }
}
