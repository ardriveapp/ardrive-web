query FirstDriveEntityWithIdOwner($driveId: String!) {
  transactions(
    first: 1
    sort: HEIGHT_ASC
    tags: [
      { name: "ArFS", values: ["0.10", "0.11", "0.12"] }
      { name: "Entity-Type", values: ["drive"] }
      { name: "Drive-Id", values: [$driveId] }
    ]
  ) {
    edges {
      node {
        owner {
          address
        }
      }
    }
  }
}
