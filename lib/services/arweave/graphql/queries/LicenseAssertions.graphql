query LicenseAssertions($transactionIds: [ID!], $owners: [String!]) {
  transactions(
    first: 100
    ids: $transactionIds
    owners: $owners
    tags: [{ name: "App-Name", values: ["License-Assertion"] }]
  ) {
    edges {
      node {
        ...TransactionCommon
      }
    }
  }
}
