query LicenseAssertions($transactionIds: [ID!]) {
  transactions(
    first: 100
    ids: $transactionIds
    tags: [{ name: "App-Name", values: ["License-Assertion"] }]
  ) {
    edges {
      node {
        ...TransactionCommon
      }
    }
  }
}
