name: pr check
on:
  pull_request:
    branches-ignore:
      - gh-pages
      - master
jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
      - uses: subosito/flutter-action@v1
        with:
          flutter-version: '3.0.5'
      - name: Build app
        run: |
          flutter pub get
          sed -i "s/REPLACE_WITH_PENDO_IOS_KEY/${{ secrets.PENDO_IOS_API_KEY }}/" lib/main.dart
          sed -i "s/REPLACE_WITH_PENDO_ANDROID_KEY/${{ secrets.PENDO_ANDROID_API_KEY }}/" lib/main.dart
          sed -i "s/REPLACE_WITH_PENDO_WEB_KEY/${{ secrets.PENDO_WEB_API_KEY }}/" lib/main.dart
          flutter pub run build_runner build
          flutter config --enable-web
          flutter build web --profile
      - name: main.dart.js cache invalidation
        run: |
          sed -i 's/main.dart.js/main.dart.js?version='"$GITHUB_SHA"'/' build/web/index.html
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_CI_SERVICE_ACCOUNT }}'
          expires: 14d
